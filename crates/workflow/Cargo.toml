[package]
name = "vortexflow-workflow"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Workflow definitions, DAG validation, and tool specifications for VortexFlow"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
petgraph = "0.7"
thiserror = "2.0"
parking_lot = "0.12"
dirs = "6.0"
